#ifndef __UDP_CASE_H___
#define __UDP_CASE_H___

#include <netinet/in.h>
#include <arpa/inet.h>
#include <sys/socket.h>
#include <netdb.h>
#include <ctype.h>

//#define SERVER_PORT 8080
//#define BUF_SIZE 1024

static unsigned int SERVER_PORT=0; /* Init */
static int BUF_SIZE=0; /* Init */
typedef struct sockaddr SA;

#endif
